aio-pika==9.4.*
asyncpg==0.30.*
# ADR-0023 — Google Cloud TTS is the default voice engine. Optional at
# runtime: with no credentials the service falls back to Piper, so the
# pipeline still runs fully offline.
google-cloud-texttospeech==2.*
